# Soul Ghost

This sample documents the `SpawnSoulGhostEffectData` style. It is intended for hit ghosts, mesh echoes, and stylized afterimages.

The package captures generic Unity renderers only. Project-specific renderer systems such as Spine should be exposed through a project adapter.
