name: Unity MCP Supervisor

on:
  push:
    branches: [main, develop]
    paths:
      - "Tools/unity-mcp-supervisor/**"
      - ".github/workflows/unity-mcp-supervisor.yml"
  pull_request:
    branches: [main, develop]
    paths:
      - "Tools/unity-mcp-supervisor/**"
      - ".github/workflows/unity-mcp-supervisor.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test:
    runs-on: windows-latest
    defaults:
      run:
        working-directory: Tools/unity-mcp-supervisor
    steps:
      - uses: actions/checkout@v7.0.1
      - uses: astral-sh/setup-uv@v9.0.0
        with:
          python-version: "3.12"
          working-directory: Tools/unity-mcp-supervisor
          enable-cache: true
      - run: uv lock --check
      - run: uv sync --locked
      - run: uv run ruff check src tests
      - run: uv run ruff format --check src tests
      - run: uv run pytest -q
      - run: uv build
